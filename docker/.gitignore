# Local, per-machine files — never commit these.
.env
# If you run setup here, settings.yml gets a real secret written in; keep the
# committed copy on its placeholder by not staging that change.
searxng/settings.yml.bak
